# BOOP context

Updated 2026-09-08. Fresh main owns shared product/ownership contracts. `SESSION_HANDOFF.md` owns current implementation/evidence. Normal app work uses `boop-unified`, one package `com.boop.alpha1` with the permanent BOOP signer. Protect historical/physically accepted checkpoints.

## Current approved pass

One signed test candidate combines accumulated Shield/wake-name repairs, cached iris-only hue and the same locked phone eyes/blink on Shield. Home = Room -> supported controllable devices using cyan chunky TV controls. Settings = configuration only. No Favourites or diagnostic/helper inventory. Focus must not shift labels or disappear during state updates. HA discovery is read-only/fail-closed, including device-inherited area membership. Never move/rename HA entities or alter HA configuration for filtering.

BOOP forever: only an additional spoken call name is stored in `boop_voice/wake_name`, labelled `BOOP's name`. Original natural BOOP phrases remain fallback. No name allowlist, new cloud dependency, replacement recognition engine or app/package/class/branding/HA/pairing/signing identity changes. UNIGRAM/tokenizer/fallback repairs need acoustic testing; previous categorical claims of the physical cause were too strong.

## Locked artwork and shared animation

Approved phone/Wall eyes, v40 landscape proportions, five-digit yellow hands and headphones remain locked. No regeneration or reimagining. Shield uses the canonical `BoopEyeLayout` and `BoopIdleBlink` helpers, copied with namespace adaptation into its internal library rather than maintained as a separate numerical recipe. TV framing preserves existing placement and uniformly scales the canonical eye pair. Idle blink shares duration, openness curve and delay; overlay visibility/power gates must not take input focus. Headphones puppetry remains unchanged.

Eye hue affects only original blue/cyan iris-ring pixels. Preserve sclera, pupil, highlights, outline and default blue. Reuse tint buffers rather than allocate a full image for every slider tick. Physical appearance is not certified by source or unit tests. Hands and 2.5D puppetry remain a later dedicated pass.

## Manual acceptance rule

Ryan owns visual and real-device acceptance unless he explicitly reverses that decision. No GitHub screenshot/golden-image tests, appearance/layout/animation judging, aesthetic source-string guards, emulator installation or launch acceptance in the unified delivery path. The previously optional post-upload process smoke is removed at his latest request. Keep fast non-visual functional tests, compilation, lint, package/signature/archive/security verification and immediate APK upload. Historical test code is retained but not selected. Documentation-only pushes do not rebuild.

## Preserved privacy and behaviour

Local HA/media control first; respect explicit room intent. Routines retain distinct execution semantics. Foreground wireless docking permits wake listening, undocked Wall is tap-to-talk. Preserve TTS/tap/settings/lifecycle microphone ownership. Shield overlay remains non-focusable/non-touchable without microphone or HA socket. No automatic user-device installs or permission grants.

Docked eyes can sleep. Proximity nudges/brief camera peeks are not continuous whole-room image recognition. Explicit mirror preview stays separate. Future horizontal inside/outside sensor rails and AC teaching remain deferred; never invent readings or mappings. Three-second chat-mode switching and accepted media/launcher behaviour stay intact.

## Evidence boundary

Delivered v43 `6cd9c67` showed modern Settings but had wrong/unreachable device placement and old Home focus. Ryan's Pixel 7 after Android 17 still handled media/blink/hue, but both wake names failed after rename. Dock state/logs were not supplied. No full OS compatibility or proven wake cause is established. Current v44 code `4044ee5` preserves concurrent `dcc7acf` fixes and awaits its exact CI/artifact/physical receipt in handoff/status.

Original detailed history remains in `docs/history/unified-v43/` and `unified/SOURCE_HEADS.md`. Publish no secrets/private addresses/screenshots/raw diagnostics. Connected GitHub work does not imply Windows worktree sync or unattended monitoring.
