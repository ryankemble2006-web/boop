# BOOP context

Updated 2026-09-08. Fresh main owns shared product/ownership contracts. SESSION_HANDOFF.md owns this app's current implementation and evidence. Normal application development belongs on `boop-unified`, one package `com.boop.alpha1` with the existing permanent signer. Protected historical and physically accepted checkpoints remain intact.

## Current approved pass

Ryan requests one signed test candidate combining the accumulated Shield and wake-name repairs with iris-only hue correction. Home is Room -> supported controllable devices with the modern cyan TV controls; Settings is configuration-only. No Favourites or diagnostic/helper inventory. Focus must not move labels or disappear during state updates. HA area discovery is read-only and fail-closed, including device-inherited membership. Do not rename/move HA entities or alter HA configuration to filter them.

BOOP remains BOOP everywhere except the additional spoken call name, stored in `boop_voice/wake_name`. Original natural BOOP phrases are permanent fallback. No approved-name list, new cloud dependency, replacement recognition engine or unrelated command/identity change. The UNIGRAM tokenizer correction and custom-stream fallback are in current source but require physical acoustic verification; earlier claims that the precise physical failure was proven were too strong.

## Standing visual-acceptance rule

Ryan explicitly owns BOOP visual acceptance, permanently unless he reverses the decision. No GitHub screenshot/golden-image comparisons, appearance/layout/animation judgement or aesthetic source-string guards. Keep compilation, focused functional tests, package/signature/integrity/security checks. Non-visual process/entry smoke is not proof of correct looks. Signed test APK upload should not wait for visual inspection. See BOOP_RULES.md.

Approved eye and five-digit yellow-hand artwork and character geometry remain locked. The eye hue preference affects only the blue/cyan iris ring, not the whites, pupil, highlights or outer eyeball. Code animates the existing assets; image generation must not reinvent approved parts. Hands/2.5D puppetry remains a later dedicated animation pass.

## Preserved privacy and behaviour

BOOP is an expressive honest puppet. Local HA/media control comes first, explicit room intent is respected, and Routines keep their existing execution semantics. Foreground wireless docking permits wake listening; undocked Wall is tap-to-talk. Preserve TTS/tap/settings/lifecycle microphone ownership. Shield overlay remains non-focusable/non-touchable without a microphone or HA socket. Do not grant permissions or install on a user's device automatically.

Docked eyes can sleep. Proximity nudges and brief camera peeks are not continuous whole-room image recognition. Explicit mirror preview remains separate; future horizontal inside/outside sensor rails and AC teaching remain deferred. No sensor mappings or capabilities are invented. Three-second chat-mode switching and accepted media/eye/blink/launcher behaviour remain intact.

## Evidence boundary

Last delivered `6cd9c67` is signed/CI-green, not an accepted wake-name release. Ryan now sees the cyan Shield Settings but reports wrong device placement, nonselectable rows and old Home focus behaviour. On Pixel 7 after Android 17, media/blink/colour still work, but both custom and BOOP wake failed after rename. Do not generalise this to full Android 17 compatibility or a proven root cause. Current source based on `848a997` awaits fresh CI/artifact and manual hardware acceptance. See handoff/status for exact receipts.

Original detailed art, privacy, product history and source receipts remain in `docs/history/unified-v43/` and `unified/SOURCE_HEADS.md`. This public repository must not receive secrets, private device data/screenshots or raw diagnostics. No laptop worktree synchronization is implied by connected GitHub work.
