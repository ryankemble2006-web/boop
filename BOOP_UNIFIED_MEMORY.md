# BOOP unified memory

Updated 2026-09-08. Read fresh main authority plus app handoff. Canonical app branch `boop-unified`, package `com.boop.alpha1`, permanent signer. Do not treat historical/primary checkouts as current app source.

## Durable decisions

- BOOP forever. One extra spoken wake name in `boop_voice/wake_name`; BOOP fallback remains. No other identity/name changes. Real-model token tests do not establish acoustic success.
- Shield Home = Room -> actual supported controllable devices. No Favourites, helpers or maintenance diagnostics. Devices do not belong in Settings. Keep cyan chunky controls, fixed focus geometry, D-pad access and focused-row retention through updates.
- HA room discovery is read-only and fail-closed, including entity/device inherited association through HA target expansion. Local filtering must not expose or operate the whole house. Compact category lookup can be a keyed object, not just an array.
- Locked phone eyes and accepted landscape proportions are also Shield's canonical eye reference. Reuse `BoopEyeLayout` and `BoopIdleBlink` source with only namespace adaptation; identical 183 ms blink curve and 3-7 second delay. TV slot framing/lifecycle may adapt without redesign. Overlay must not take focus. Do not change the locked headphones renderer.
- Iris hue changes only original blue/cyan ring pixels; preserve whites, pupils/reflections, outline and default bitmap. Use cached tint buffers. Ryan owns visual evaluation, not an image generator.
- No automated visual/aesthetic-source or device/emulator launch/install acceptance in the unified delivery workflow. Ryan explicitly broadened the previous no-visual-check rule. Keep focused functional/compile/lint/signature/package/integrity/security checks; upload immediately. Historical tests remain but are not selected. Do not silently re-enable removed acceptance stages.
- Foreground wireless charging permits continuous wake. Undocked is tap-to-talk. Preserve mic ownership; no always-on handheld workaround. No automatic installs or permission grants.
- Hands/2.5D animation, future HA sensor rails and AC teaching remain separate/deferred. Preserve existing media, voice, launcher, permissions and signing.

## Current evidence

v44 code `4044ee55b5a39e2a220ee897de0393b796c29a5f` is published on top of concurrent `dcc7acf32acd2cf44d25e1f88fce7d388695e030`; no concurrent room/iris work was overwritten. Run `34192698906` is in progress, with initial contracts/materialization passed. Local shared-eye adapter syntax and full source-anchor checks passed. Finished build/artifact and physical acceptance are not yet claimed.

Delivered `6cd9c67` was CI-green but contained the old custom tokenizer and now has Ryan's failed custom/fallback wake report. Subsequent source has tokenizer/fallback repairs; the physical cause remains unproven. Pixel 7's Android 17 media/blink/hue checks are partial observation only. Retest acoustic wake foreground on wireless dock; handheld wake is deliberately disarmed.

Protected accepted rollback `e746affbb82b577cef2f1cf6e731dff186c8f881` remains. Prior exact artifacts and history live in `SESSION_HANDOFF.md`, `docs/history/unified-v43/` and `unified/SOURCE_HEADS.md`. No Windows worktree sync, device installation or background monitoring was established. Record the final new run/artifact receipt before calling this ready.
