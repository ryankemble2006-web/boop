<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-feature android:name="android.software.leanback" android:required="true" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Sockets connect only to this device's loopback ADB daemon, never an Internet endpoint. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Allows the explicitly enabled, bounded CLEAN START job and persistent TURBO state to resume after a real boot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required only while verified TURBO is active so Android thermal status remains watched. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Thermal-watchdog foreground-service notification only. CLEAN START itself is silent. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Granted only by the user's explicit local ADB setup after Android RSA approval. -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <queries>
        <package android:name="com.android.tv.settings" />
        <package android:name="com.android.settings" />
        <intent><action android:name="android.settings.DISPLAY_SETTINGS" /></intent>
        <intent><action android:name="android.settings.SOUND_SETTINGS" /></intent>
        <intent><action android:name="android.settings.ACCESSIBILITY_SETTINGS" /></intent>
        <intent><action android:name="android.settings.APPLICATION_DEVELOPMENT_SETTINGS" /></intent>
        <intent><action android:name="android.settings.APPLICATION_SETTINGS" /></intent>
        <intent><action android:name="android.settings.INTERNAL_STORAGE_SETTINGS" /></intent>
        <intent><action android:name="android.settings.STORAGE_SETTINGS" /></intent>
        <intent><action android:name="android.settings.WIFI_SETTINGS" /></intent>
        <intent><action android:name="android.settings.NETWORK_OPERATOR_SETTINGS" /></intent>
        <intent><action android:name="android.settings.DEVICE_INFO_SETTINGS" /></intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application android:name=".TurboApplication" android:theme="@style/Theme.ShieldTurbo" android:label="@string/app_name" android:banner="@drawable/tv_banner" android:allowBackup="false" android:supportsRtl="true">
        <activity android:name=".MainActivity" android:screenOrientation="landscape" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".performance.HeadroomActivity" android:screenOrientation="landscape" android:exported="false" />
        <activity android:name=".power.PowerActivity" android:screenOrientation="landscape" android:exported="false" />
        <activity android:name=".startup.StartupManagerActivity" android:screenOrientation="landscape" android:exported="false" />
        <receiver android:name=".cleanstart.CleanStartBootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <receiver android:name=".performance.TurboBootReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <service android:name=".BrightnessService" android:exported="false" />
        <service
            android:name=".cleanstart.CleanStartJobService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />
        <service
            android:name=".performance.TurboThermalWatchdogService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps Android thermal-status protection active while SHIELD TURBO stock performance mode is enabled." />
        </service>
    </application>
</manifest>
