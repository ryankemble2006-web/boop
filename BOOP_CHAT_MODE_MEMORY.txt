BOOP WALL CHAT MODES: CURRENT IMPLEMENTATION MEMORY
Updated 2026-09-07. Owning variant: boop-relay-reviewed-v34.

Reason: keep house/media control independent of exhausted conversation credits.
Hold eyes for 3 seconds past the playful hold: OpenCode / Free Chat / Native Chat / Cancel.
OpenCode remains default. Selection persists; same gesture reverses it. No new
settings screen, permissions, cloud device control or timed routines.
Local executes exactly once first. Only genuine NO_MATCH may invoke conversation.

Free Chat: visible browser session, copied question for manual paste/send and its
own login/limits. Native Chat: authenticated Worker/OpenAI request-response using
existing BOOP voice/eyes/follow-up. Short errors stay conversation-specific and
cannot clear HA credentials. No provider key in Android and no hidden scraping.

Implementation tasks 1-5 complete; task 6 signed setup delivery/full CI completed.
Built `453bda9ec5bcd12f44fe50d63c7db5ce7a718295`, run 34084002048, job 101624466469, artifact 10004727824.
APK SHA256 1bb448d6f458cf4b527a69f7137f65ef3dd5a1a1da7dfab958b5cfe8efaeba97.
177 local source/Python/JVM checks and 13 mock Worker checks passed; actual Android
build/unit/signing and full normal emulator gates passed. Final visual profile
720x1560@280dpi preserves dp/aspect/time. Live View renders at observed natural blink phases, not forced poses.
The earlier incomplete high-resolution captures and initial unknown wake failure
remain historical evidence; do not erase or recast them as physical failures.

Worker is NOT deployed. APK configuration is setup-required; no real API conversation
or physical v34 test. Native Chat cannot work from this APK alone. Private Worker
and build configuration/rebuild is still required. API use is separately billed,
not the consumer ChatGPT credit pool. Configured APK relay tokens are extractable;
public CI refuses token-bearing builds. Private single-owner prototype only; no
per-device enrollment, independent revocation or hard spending cap yet.

Concurrency: another session advanced boop-wall-free-chat-wip with its own relay.
This variant is isolated, not merged. Match APK+Worker+receipt on the same branch;
v34 label alone is not identity. Main 85d08eb records ownership. Read live handoffs.
No subagent launcher here; serial implementation/review, not independent agents.

Preserved: raw MainActivity, voice/wake/HA/media, v33 183ms blink, 1.5x bold paste
instructions, permissions and permanent signer. No physical installation/settings
changes. Wall checkpoint 595e1da, Shield Home/Routines and Launcher unchanged.
Ryan physically observed v31 browser handoff and v32 blinking; do not extrapolate
those reports to v34 native success. Prior receipts/handoffs remain archived.

Next: private matching Worker/build setup, then actual native speech/follow-up,
failures, local media during relay outage and physical gesture/sleep acceptance.
SESSION_HANDOFF.md and docs/BOOP-WALL-V34-BUILD-RECEIPT.md hold exact evidence.
"Update memory" remains docs-only unless code/install/permission/signing separately
requested. Already-open Work tasks must fetch/reread; no laptop sync is implied.
