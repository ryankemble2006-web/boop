BOOP WALL CHAT MODE: CURRENT FEATURE MEMORY
Updated 2026-09-07; supplements the preserved dated BOOP_MEMORY.txt history.

Why: OpenCode credit exhaustion broke open-ended conversation while local media
and house control remained useful. Ryan approved a reversible fallback rather
than making the whole puppet depend on that credit pool.

Approved UX: hold the eyes for three seconds, past the playful animation; menu
Chat mode -> OpenCode / Free Chat / Cancel. Default OpenCode. Remember the choice
across restarts; same gesture restores OpenCode. No permanent settings button.

Boundary: local control always runs first, regardless of mode. Only a genuine
local NO_MATCH can use the selected conversation route. Do not send house auth
failures, offline targets or device-control errors to a web chatbot.

Honesty: Free Chat is a visible browser ChatGPT session with its own login and
limits, not unlimited service, an embedded consumer API, or spoken answers
behind the BOOP eyes. The question is copied for manual paste/send; the menu
explains this. No scraping, injection, account-cookie extraction or direct key.

Preservation: Wall and Launcher remain separate packages. Existing swipe and
short playful hold coexist with the longer mode-menu hold. Movement, multi-touch,
release, focus loss and backgrounding cancel pending activation in source.
Physical interaction acceptance is still required before promotion.

Build: scripts/materialize-android.sh applies wake, toast, chat and idle-blink
patches. Raw MainActivity stays frozen; do not mistake it alone for the built app.
Existing permanent GitHub signer only. Owning branch boop-wall-free-chat-wip.
Physical checkpoint 595e1da remains unchanged.

CURRENT CANDIDATE: v33 / 0.4.13-wall-blink-speed.
Application/build commit e3507bde3f296dcb419a1dcef0faf735c7243525.
Exact artifact/signing receipt: docs/BOOP-WALL-V33-BUILD-RECEIPT.md.
Ryan confirmed v32: "he blinks just fine". He requested only 20 percent faster
animation and expressly no tests. DURATION_MS changes 220 -> 183 (220/1.2 rounded).
The irregular 3-7 second interval, geometry, enlarged text, original30s sleep,
voice/wake/HA, gestures, signer and permissions do not change.
No runtime tests/emulator runs for v33; build/sign-only is not full-CI-green.
The optional [boop-build-only] workflow marker requires an explicit user request;
normal pushes/manual runs retain full checks. Docs use [skip ci] for this handoff.

v32's new automated natural-blink capture failed, despite earlier partial checks;
its cause remains unresolved. Ryan's physical blink observation is positive
independent evidence, not a retroactive CI pass. v32 text visibility/size/clipping,
sleep and unreported orientations remain unverified. v33 speed awaits his use.
Exact v32 history/receipt: docs/BOOP-WALL-V32-HANDOFF.md.

Ryan confirmed v31 copied his query, opened a new chat and instructed him to paste.
That is limited positive physical browser-handoff evidence, not every scenario.
Last full-green v31 is0ceb97b/run34071614834/artifact10000728933; detailed history
is preserved at docs/BOOP-WALL-V31-HANDOFF.md. Keep physical checkpoint595e1da and
preserved Wall lineage unchanged; don't silently promote later candidates.

Next: record Ryan's actual faster-blink feedback. Do not run deferred visual tests
under this no-tests request. Cross-device main@2912a19 maps the owning candidate.
Work must fetch/reread the owning branch, not assume local/main app files are newest.
"Update memory" is docs-only unless Ryan separately requests implementation,
installs, permissions or signing. No concurrent work was overwritten; GitHub-only
access is not laptop synchronization. Docs-only commits do not change the APK.
