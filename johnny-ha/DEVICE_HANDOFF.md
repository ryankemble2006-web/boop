# Johnny HA Lab v15 — fast HA reaction installed, physical latency verdict pending

Updated 2026-09-16. Owner `johnny-ha-native-v8`. Logic source `8f8b7b0b9c1242020cd6049aa557606403a9714a`; versioned source `afe84ea41f3e04e105166fe4c7dc3c3ce1431ae0`. CI `35060689367` SUCCESS. Package `local.johnnycastaway.halab`, versionCode `15`, versionName `1.14-reborn-ha-fast-poll`.

Ryan asked for fan/HA reactions to start much faster and cancel ordinary Johnny activity as soon as practical. Root cause was the Java HA snapshot client polling every2seconds; native ADS/walk pre-emption already exits ordinary routines at its existing safe frame/cleanup boundary. Healthy polling is now250ms. Provider-unavailable backoff preserves the prior roughly10-second retry cadence instead of hammering BOOP during an outage. The accepted eight-second wind episode, OI timing/pixels, music reactions and native cancellation/ownership logic are unchanged.

TDD evidence: test-only commit `16a1420bc1bafb1077a75e6ef430789bc94bc2a9`, run `35060022954`, failed exactly because `JohnnyPollPolicy.java` did not exist. Final v15 run `35060689367` passed HA edge/poll policy tests, native policy tests, pinned-source patch tests,100-cycle ADS cancellation/ownership sanitizer coverage, arm64 native build, Android View compilation and artifact upload.

Shield install evidence: prior installed v14 was pulled first as rollback and matched SHA256 `8c7f4a95dcca4bc23da2e46e8bc45ac34a10dac92a9504ad28bc555c7a2daf2c`, signer `785ce5d046b1947a48dd40fbbd4e83aa1e69665d5b89426af5d9abc215c05215`. The first v15 package attempt was rejected before replacement because `resources.arsc` was deflated; the archive was rebuilt with stored/aligned entries matching the known-good v14 packaging shape, using the same CI artifact, approved private assets and existing signer. Final signed APK SHA256 `3810ac838ecb0c9e7d10a61f38551b44a794b33d9b239ed29d8d8786e5ddc370`; `adb install -r` succeeded and a fresh pull of installed `base.apk` matched that hash exactly. Selected/default screensaver remains `local.johnnycastaway.halab/local.johnnycastaway.shield.JohnnyDream`, screensavers enabled, and JohnnyMusicListener remained authorized. End-to-end fan latency is not physically accepted until Ryan tests the live screensaver.

# 2026-09-16 lights-off follow-up

Ryan physically confirmed the first light-urgency candidate made lights ON immediate, but lights OFF still left the current animation visible for a few seconds before OI. This isolates the remaining issue to OI scheduling, not HA polling or day/night state detection.

Current follow-up design: keep the environment-only interrupt immediate, but delay only the OI request by a tested 150 ms Android render window. This gives Johnny a chance to publish a real night frame before OI takes actor ownership. The delayed callback is session/lifecycle guarded; lights-on before expiry causes native OI rejection, so stale OI does not start. Do not claim this follow-up works physically until CI passes, the candidate is installed, and Ryan tests lights-off again.
