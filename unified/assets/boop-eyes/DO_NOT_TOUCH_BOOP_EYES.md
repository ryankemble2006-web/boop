# BOOP permanent default eye master

`boopApprovedEyes.png` is BOOP's permanent default source image.

DO NOT edit, flatten, recompress, regenerate, recreate, crop, resize on disk, recolor/recolour the master, threshold or flood-fill it, reconstruct its transparency, or substitute a close-enough version.

Exact identity:
- 1774 x 887
- RGBA with the supplied alpha preserved
- 936,803 bytes
- SHA-256 `ffbd67af22c2f11b4a109bd83e8c5197c266a777df2fbc97ce1ab5163e9fed22`
- Git blob `f95375356b20297fa2b27ab8887f65d4cce5c7fd`

Runtime code may scale, position, blink, mask or animate this source non-destructively. A user eye-colour feature may affect the iris at runtime, but it must never rewrite or flatten the master bytes.

Replace this image only after Ryan explicitly approves a new permanent master and its exact identity is recorded.
